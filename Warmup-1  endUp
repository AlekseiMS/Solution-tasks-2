public String endUp(String str) {
  if (str.length() >= 3) {
  String st = str.substring(0, str.length() - 3);
  String s = str.substring(str.length() - 3).toUpperCase();
  return st + s;
  }
  return str.toUpperCase();
}
